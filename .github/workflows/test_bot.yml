name: Test Discord Bot Workflow

on:
  workflow_dispatch:

jobs:
  test_job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Print message
      run: echo "✅ Workflow triggered successfully by Discord bot!"